#ifndef SERVER_H
#define SERVER_H


class Server
{
    public :
        Server();
        Server(const &Server);
        ~Server();
        Server &operator=(const &Server);
    private :

};

#endif // SERVER_H
